# Enable Redis caching
cache-enabled yes

# Set the Redis cache backend
cache-backend redis

# Configure the Redis server connection
cache-redis-server 127.0.0.1
cache-redis-port 6379
cache-redis-db 0

# Set the Redis cache prefix
cache-prefix "wp_"
